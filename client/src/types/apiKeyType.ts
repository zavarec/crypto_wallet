export type ApiKeyType = {
  id: number;
  name: string;
  apiKey: string;
  apiSecret: string;
};

// export type StockState = {
//   status: 'fetching' | 'idle' | 'error';
//   data: StockType[];
// };
