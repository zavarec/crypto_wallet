import React from 'react';
import { useAppSelector } from '../../hooks/useReduxHook';

export default function StockPart(): JSX.Element {
  // const stocks = useAppSelector((state) => state.)
  return <div>StockPart</div>;
}
